import { DifficultePipe } from './difficulte.pipe';

describe('DifficultePipe', () => {
  it('create an instance', () => {
    const pipe = new DifficultePipe();
    expect(pipe).toBeTruthy();
  });
});
